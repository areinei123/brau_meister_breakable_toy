<div class='row'>
  <%= form_tag ingredients_path, method: :get do %>
    <h4>From U.S.A</h4>
    <%= select_tag(:name, options_from_collection_for_select(@us_hops, :id, :name), selected: :name, include_blank: "Select") %>
    <h4>From England</h4>
    <%= select_tag(:name, options_from_collection_for_select(@england_hops, :id, :name), selected: :name, include_blank: "Select") %>
    <h4>From Australia</h4>
    <%= select_tag(:name, options_from_collection_for_select(@australia_hops, :id, :name), selected: :name, include_blank: "Select") %>
    <h4>From Germany</h4>
    <%= select_tag(:name, options_from_collection_for_select(@german_hops, :id, :name), selected: :name, include_blank: "Select") %>
    <h4>From Czech Republic</h4>
    <%= select_tag(:name, options_from_collection_for_select(@czech_hops, :id, :name), selected: :name, include_blank: "Select") %>
    <h4>From Japan</h4>
    <%= select_tag(:name, options_from_collection_for_select(@japan_hops, :id, :name), selected: :name, include_blank: "Select") %>
    <h4>From New Zealand</h4>
    <%= select_tag(:name, options_from_collection_for_select(@new_z_hops, :id, :name), selected: :name, include_blank: "Select") %>
    <h4>From Slovenia</h4>
    <%= select_tag(:name, options_from_collection_for_select(@slovenia_hops, :id, :name), selected: :name, include_blank: "Select") %>
  <% end %>
</div>